(()=>{const deck=document.querySelector('[data-ldp-deck]');if(!deck)return;const slides=[...deck.querySelectorAll('[data-ldp-slide]')];let index=0;const progress=document.querySelector('[data-ldp-progress]');const sync=()=>{slides.forEach((s,i)=>s.setAttribute('aria-current',String(i===index)));if(progress)progress.style.width=`${((index+1)/slides.length)*100}%`;document.querySelector('[data-ldp-counter]')?.replaceChildren(`${index+1} / ${slides.length}`);try{localStorage.setItem('ldp-slide',String(index));}catch{}};const go=n=>{index=Math.max(0,Math.min(slides.length-1,n));sync()};document.addEventListener('keydown',e=>{if(['ArrowRight','ArrowDown','PageDown',' '].includes(e.key)){e.preventDefault();go(index+1)}if(['ArrowLeft','ArrowUp','PageUp'].includes(e.key)){e.preventDefault();go(index-1)}if(e.key==='o')deck.classList.toggle('ldp-overview');if(e.key==='n')deck.classList.toggle('ldp-show-notes')});let wheelLock=false;document.addEventListener('wheel',e=>{if(wheelLock||innerWidth<769)return;wheelLock=true;go(index+(e.deltaY>0?1:-1));setTimeout(()=>wheelLock=false,500)},{passive:true});let startX=0;document.addEventListener('touchstart',e=>startX=e.touches[0].clientX,{passive:true});document.addEventListener('touchend',e=>{const dx=e.changedTouches[0].clientX-startX;if(Math.abs(dx)>50)go(index+(dx<0?1:-1))},{passive:true});go(Number(localStorage.getItem('ldp-slide')||0));})();
